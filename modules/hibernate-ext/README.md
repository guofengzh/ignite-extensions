# Apache Ignite Hibernate Extension

## Important!!

First run the following task to convert the apache ignite jta module's javax.transaction to jakarta.transaction:
```
cd jta-jakarta
mvn mvn install
```
